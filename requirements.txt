# CodeFormer -- modernised dependency set
# Verified target: Python 3.10-3.13, PyTorch >= 2.2, NumPy >= 2.0, SciPy >= 1.15

addict>=2.4.0
ffmpeg-python>=0.2.0
gdown>=5.2.0
lmdb>=1.4.1
lpips>=0.1.4
numpy>=1.26,<3
opencv-python-headless>=4.9.0.80
packaging>=23.2
Pillow>=10.2.0
PyYAML>=6.0.1
requests>=2.31.0
scikit-image>=0.22.0
scipy>=1.11
tensorboard>=2.16.0
torch>=2.2.0
torchvision>=0.17.0
tqdm>=4.66.0
yapf>=0.40.2

# Removed from the original list:
#   future     -- Python 2 compatibility shim, dead on Python 3
#   tb-nightly -- unpinned nightly build, replaced by stable `tensorboard`
#   opencv-python -- swapped for the headless build (no libGL on servers);
#                    install `opencv-python` instead if you need cv2.imshow
